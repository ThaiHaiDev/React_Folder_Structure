export enum RouteDictionary {
  Home = '/',
  SignIn = '/sign-in',
  SignUp = '/sign-up',
  ForgotPassword = '/forgot-password',
  GetStart = '/get-start',
  Users = '/users',
  Error = '/error',
  ResetNewPassword = '/reset-new-password',
}

export enum CustomerRoutePath {
  Detail = '/detail/:idHome',
}
